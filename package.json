{
  "name": "playground",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "clean": "turbo clean && rm -rf node_modules",
    "dev:main": "turbo dev --filter=main",
    "dev:worldcup": "turbo dev --filter=worldcup"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.20",
    "turbo": "^2.3.3",
    "typescript": "^5.7.2"
  },
  "packageManager": "pnpm@9.15.1",
  "engines": {
    "node": ">=20"
  }
}
